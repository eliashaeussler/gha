name: 'Build documentation'

inputs:
  command:
    description: 'npm script to render pages'
    default: docs

runs:
  using: composite
  steps:
    - name: 'Setup Node'
      uses: eliashaeussler/gha/.github/actions/setup-node@7d9e57a9d8d695347983845d128e83dff10bcde7 # main

    - name: 'Install npm packages'
      uses: eliashaeussler/gha/.github/actions/npm-install@7d9e57a9d8d695347983845d128e83dff10bcde7 # main

    - name: 'Render documentation'
      env:
        BUILD_COMMAND: ${{ inputs.command }}
      run: npm run "$BUILD_COMMAND"
      shell: bash
