name: 'Run tests with Composer'

inputs:
  command:
    description: 'Composer command to execute'
    default: test

runs:
  using: composite
  steps:
    - name: 'Run tests'
      run: composer ${{ inputs.command }}
      shell: bash
