name: 'DevSecOps'

on:
  workflow_call:
    secrets:
      DEVGUARD_TOKEN:
        description: 'DevGuard API token'
        required: true

    inputs:
      devguard-asset-name:
        description: 'Asset name of the repository used in DevGuard'
        required: true
        type: string

permissions:
  contents: read

jobs:
  secret-scanning:
    name: 'Secret scanning'

    secrets:
      devguard-token: ${{ secrets.DEVGUARD_TOKEN }}

    uses: l3montree-dev/devguard-ci-components/.github/workflows/secret-scanning.yml@54e6c85fd3d9c041522b4290d5d41879992574a2 # v1.13.1
    with:
      devguard-asset-name: ${{ inputs.devguard-asset-name }}

  dependency-scanning:
    name: 'Dependency scanning'

    secrets:
      devguard-token: ${{ secrets.DEVGUARD_TOKEN }}

    uses: l3montree-dev/devguard-ci-components/.github/workflows/software-composition-analysis.yml@54e6c85fd3d9c041522b4290d5d41879992574a2 # v1.13.1
    with:
      devguard-asset-name: ${{ inputs.devguard-asset-name }}

  sast:
    name: 'SAST'

    secrets:
      devguard-token: ${{ secrets.DEVGUARD_TOKEN }}

    uses: l3montree-dev/devguard-ci-components/.github/workflows/static-application-security-testing.yml@54e6c85fd3d9c041522b4290d5d41879992574a2 # v1.13.1
    with:
      devguard-asset-name: ${{ inputs.devguard-asset-name }}
